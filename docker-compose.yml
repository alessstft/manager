services:
  web:
    build: .
    ports:
      - "8080:8000"
    volumes:
      - db_data:/app/data
      - media_data:/app/tasks/media
    environment:
      DJANGO_DB_PATH: /app/data/db.sqlite3
    restart: unless-stopped

volumes:
  db_data:
  media_data:
